<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard1.3</TargetFramework>
    <PackageId>ConsoleMenu-simple</PackageId>
    <Authors>lechu445</Authors>
    <Product>ConsoleMenu-simple</Product>
    <Description>A simple, highly customizable, DOS-like console menu</Description>
    <PackageReleaseNotes>
      - add property Items and CurrentItem that you can access current menu item `menu.Items` and `menu.CurrentItem`
      - allow change Name and Action of MenuItem via setter `.Add("Change me", (thisMenu) => thisMenu.CurrentItem.Name = "I am changed!")`
      - allow do action then close menu `.Add("Action then Close", (thisMenu) => { SomeAction("Close"); thisMenu.CloseMenu(); })`
    </PackageReleaseNotes>
    <PackageProjectUrl>https://github.com/lechu445/ConsoleMenu</PackageProjectUrl>
    <Version>2.3.0.0</Version>
    <RepositoryUrl></RepositoryUrl>
    <PackageTags>console, menu, simple</PackageTags>
  </PropertyGroup>

</Project>
